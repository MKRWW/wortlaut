"""Pipeline-Layer — Orchestrierung fetch → hash → dedup → archiv → WORM → insert.

Provenienz vor Verarbeitung (R-CORE-02). Komponiert ingest/evidence/archive/store;
niemand importiert pipeline (R-ARCH-02).
"""
